(function() {var implementors = {};
implementors["sda_client_store"] = ["impl Keystore for <a class=\"struct\" href=\"sda_client_store/struct.Filebased.html\" title=\"struct sda_client_store::Filebased\">Filebased</a>",];

            if (window.register_implementors) {
                window.register_implementors(implementors);
            } else {
                window.pending_implementors = implementors;
            }
        
})()
